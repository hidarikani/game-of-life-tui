{
  "tasks": {
    "run:watch": "deno run --watch src/main.ts",
    "run:once": "deno run src/main.ts --once",
    "run:agent": "deno run src/main.ts --once"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
